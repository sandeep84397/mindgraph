---
description: "Initialize a new MindGraph knowledge base"
---

Use the mindgraph skill to initialize a knowledge base. Ask the user where to create it and whether this is a standalone knowledge base or a project plugin.

Run: `python3 -m tools init <path> --mode <standalone|project>`

Then start the watcher: `python3 -m tools watch <path> start`
